# 网页部分

网页与部署相关内容集中在：

- `web-ui/`

主要包含：

- `app/`
  - Vite + React 前端
- `workers/`
  - Cloudflare Worker
- `webhook.js`
  - GitHub 自动部署 webhook
- 部署和产品定位文档

如果要继续做机器人网页，优先从：

1. `web-ui/WEB_UI_SUMMARY.md`
2. `web-ui/WEB_PROJECT_PLAN.md`
3. `web-ui/app/src/`
开始。
